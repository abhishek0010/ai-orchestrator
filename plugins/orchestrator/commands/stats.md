Show token savings summary for the ai-orchestrator pipeline.

Run:

```bash
bash ~/.claude/stats.sh "$ARGUMENTS"
```

Pass a period as the argument: `day`, `week`, `month`, or leave empty for all-time totals.
